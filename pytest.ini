[pytest]
# The Godot suite lives in tests/*.gd and is run by ./run-tests.sh; pytest owns
# the Python halves — the local assistant helper and the hosted campaign API.
testpaths = tests/assistant tests/service
addopts = -q
